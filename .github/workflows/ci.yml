name: CI
on:
  push:
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - run: dotnet restore CompanyGuard.slnx
      - run: dotnet build CompanyGuard.slnx --no-restore -c Release
      - run: dotnet test tests/CompanyGuard.Tests/CompanyGuard.Tests.csproj --no-build -c Release
